/**
 * Basic test file for types package
 * This ensures the package can be tested without errors
 */

describe('Types Package', () => {
  it('should export types without errors', () => {
    // This test simply verifies the package can be imported
    expect(true).toBe(true);
  });
}); 